VINCULO — Correcao: pop-up de MFA so no login

Antes: o pop-up aparecia a cada carregamento/refresh, mesmo com a
sessao ja aberta.
Agora: aparece SOMENTE no momento do login (primeiro acesso). Em
refresh/reabertura com sessao aberta, nao aparece mais.
(Continua respeitando o "Lembrar daqui 30 dias" e quem ja tem MFA.)

1 arquivo (apps/web/src/App.tsx). Sem migracao, so o front.

CMD, na pasta do repo:
  git add .
  git commit -m "Fix pop-up MFA so no login"
  git push
(So o front muda; nao precisa npm run deploy da API, mas nao custa.)
Teste em aba anonima (Ctrl+Shift+R).
