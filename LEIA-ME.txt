VINCULO — Agenda: visoes Dia/Semana/Mes + busca

Novidades:
 - Alternar entre DIA, SEMANA e MES (botoes no topo direito da agenda).
 - Campo de BUSCA por nome do paciente (filtra a visao atual).
 - Navegacao (< Hoje >) se adapta ao modo (dia/semana/mes).
 - Visao de MES: calendario com os agendamentos por dia; clique num dia
   para marcar, clique num agendamento para editar.

3 arquivos, so o front. Sem migracao, sem mudanca de API.

CMD, na pasta do repo:
  git add .
  git commit -m "Agenda dia/semana/mes + busca"
  git push
(So o front; nao precisa npm run deploy da API.)
Teste em aba anonima (Ctrl+Shift+R).
