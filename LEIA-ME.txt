VINCULO — CTA "Criar conta gratis" na tela de login

- Botao grande e chamativo (laranja, com selo "7 dias gratis" e
  "sem cartao de credito") na tela de login, que leva ao cadastro.
- Efeito de brilho no hover.

3 arquivos, so o front. Sem API, sem migracao.

CMD, na pasta do repo:
  git add .
  git commit -m "CTA criar conta gratis"
  git push
(So o front; nao precisa deploy da API.)
Teste em aba anonima (Ctrl+Shift+R).
