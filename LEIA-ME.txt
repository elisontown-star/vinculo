VINCULO — CTA de cadastro (v2, mais profissional)

- Botao "Criar conta gratis" agora CENTRALIZADO, com BORDA e visual
  mais limpo. O "7 dias gratis . sem cartao" virou uma linha discreta
  abaixo do botao (sem o selo sobreposto).

3 arquivos, so o front. Sem API, sem migracao.

CMD, na pasta do repo:
  git add .
  git commit -m "CTA cadastro v2"
  git push
Teste em aba anonima (Ctrl+Shift+R).
