VINCULO — MFA opcional (remove obrigatoriedade)

Antes: cadastro e login FORCAVAM configurar o MFA antes de entrar.
Agora: o usuario entra direto (com email + senha).
  - Quem JA ativou o MFA (ex.: super admin) continua sendo desafiado
    normalmente pelo codigo. Nada muda para eles.
  - Novos usuarios e quem nao tem MFA entram sem a etapa obrigatoria.

1 arquivo (apps/api/src/routes/auth.ts). Sem migracao. So a API muda.

CMD, na pasta do repo:
  git add .
  git commit -m "MFA opcional"
  git push
Depois, publique a API:  cd apps\api  &&  npm run deploy
(so a API muda, mas nao custa republicar o front tambem)
