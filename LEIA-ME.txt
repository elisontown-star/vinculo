VINCULO — Codigo de empresa no Super Admin

- O codigo de empresa (VTX-XXXXXXXX) agora aparece no portal super admin:
  na lista de clinicas (abaixo do nome) e no detalhe da clinica.

2 arquivos, so o front. Sem API, sem migracao.
(O backend ja devolvia o companyCode; so faltava exibir.)

CMD, na pasta do repo:
  git add .
  git commit -m "Codigo de empresa no super admin"
  git push
Teste em aba anonima (Ctrl+Shift+R).
