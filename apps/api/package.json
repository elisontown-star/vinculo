{
  "name": "@vinculo/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:apply:local": "wrangler d1 migrations apply vinculo --local",
    "db:apply:remote": "wrangler d1 migrations apply vinculo --remote",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.2",
    "@vinculo/db": "*",
    "drizzle-orm": "^0.36.4",
    "hono": "^4.6.14",
    "otpauth": "^9.5.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "drizzle-kit": "^0.28.1",
    "typescript": "^5.7.2",
    "wrangler": "^3.95.0"
  }
}
